[project]
name = "trapo-workbench"
version = "0.1.0"
description = "Release tooling for the Trapo RAG Ingest Workbench"
requires-python = ">=3.14.6"
dependencies = []

[dependency-groups]
dev = [
    "ruff>=0.15.20",
]
